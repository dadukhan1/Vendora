/** @format */

import { createTheme } from "@mui/material";

export const customTheme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#0F52FF",
    },
    secondary: {
      main: "#F8FAFC",
    },
  },
});
